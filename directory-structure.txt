architect-ai/
│
├── .gitignore
├── README.md
├── requirements.txt
├── pyproject.toml      # updated for the new name and flat structure
├── MANIFEST.in         # to ensure assets are included in the package
│
├── app.py              # main app file (must have a main() function)
├── ai_service.py       # handles AI service calls
├── config.py           # manages configuration (API key)
│
└── assets/
    └── icon.ico        # application icon
